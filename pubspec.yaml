name: tripper
description: Local-first travel companion — trips, document vault, places.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  drift: ^2.20.0
  sqlite3_flutter_libs: ^0.5.24
  path_provider: ^2.1.4
  path: ^1.9.0
  go_router: ^14.3.0
  intl: any
  uuid: ^4.5.0
  file_picker: ^8.1.2
  open_filex: ^4.5.0
  local_auth: ^2.3.0
  receive_sharing_intent: ^1.8.1
  screen_brightness: ^2.1.0
  wakelock_plus: ^1.2.5
  pdfx: ^2.6.0
  google_maps_flutter: ^2.9.0
  latlong2: ^0.9.1
  flutter_earth_globe: ^2.2.1
  image_picker: ^1.2.0
  photo_view: ^0.15.0
  http: ^1.2.0
  shared_preferences: ^2.3.2
  archive: ^3.6.1
  share_plus: ^10.1.1
  flutter_local_notifications: ^18.0.1
  timezone: ^0.9.4
  google_mlkit_text_recognition: ^0.13.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  drift_dev: ^2.20.0
  build_runner: ^2.4.13
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  generate: true

  # Journal globe texture — bundled, never fetched at runtime (offline
  # rule, SPEC §3.1.2). See assets/globe/README for provenance.
  assets:
    - assets/globe/

  # Bundled, never fetched at runtime (offline rule, SPEC §3.1.2).
  # Fraunces ships only 400/600 as statics — styles map to those exactly so
  # Flutter never synthesises a weight.
  fonts:
    - family: Fraunces
      fonts:
        - asset: assets/fonts/Fraunces-Regular.ttf
        - asset: assets/fonts/Fraunces-SemiBold.ttf
          weight: 600
    - family: IBM Plex Sans
      fonts:
        - asset: assets/fonts/IBMPlexSans-Regular.ttf
        - asset: assets/fonts/IBMPlexSans-Medium.ttf
          weight: 500
        - asset: assets/fonts/IBMPlexSans-SemiBold.ttf
          weight: 600
    - family: IBM Plex Mono
      fonts:
        - asset: assets/fonts/IBMPlexMono-Regular.ttf
        - asset: assets/fonts/IBMPlexMono-Medium.ttf
          weight: 500
